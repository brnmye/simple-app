type Todo @model {
  id: ID!
  name: String!
  description: String
}

type Person @model {
  id: ID!
  name: String!
  age: Int!
  posts: [Post] @connection
}

type Post @model {
  id: ID!
  title: String!
  content: String!
  created_at: String!
}
