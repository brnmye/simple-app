type Todo @model {
  id: ID!
  name: String!
  description: String
}

type Person @model {
  name: String!
  age: Int!
  posts: [Post!]!
}

type Post @model {
  title: String!
  content: String!
  author: Person!
}
